name: archbase_demo
description: "App demo do framework archbase_flutter — visitas a PDVs com offline-first, GPS, foto e sync."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  archbase_flutter:
    path: ..
  lucide_icons_flutter: ^1.2.0
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
